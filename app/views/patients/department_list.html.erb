<h3> Department List</h3>
<table>
  <tr>
    <th>Name</th>
  </tr>
  <% @departments.each do |department| %>
  <tr>
    <td><%=h department.name %></td>
    <td><%= link_to 'Show', department %></td>
  </tr>
  
<% end %>
</table>
<br />
<%= link_to "Back", view_patients_path  %>